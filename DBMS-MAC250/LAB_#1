import mysql.connector

try:
    conn = mysql.connector.connect(
        host = 'localhost',
        user = 'root',
        password = '8551649',
        database = 'premiere'
    )
    if conn:
        print('Connection has been build')

    cursor = conn.cursor()
    if cursor:
        print('Cursor has been build')

#List the part number, description, and price for parts.

except Exception as e:
    raise Exception(f"Job Faild {e}")

finally:
    if cursor:
        cursor.close()
        print('Cursor has been closed')
    if conn:
        conn.close()
        print('Connection has been closed')
